---
topic: Rendering library installation, usage and complete API documentation
updated: 2026-09-15T01:18
---

- (direction) User invokes /bmad-spec: prepare the documentation page about the rendering library, installation through Go modules, usage, and all available APIs.
- (decision) Headless express distillation; derive slug rendering-library-documentation from the requested subject. Produce a specification, not the implemented documentation page.
- (note) Customization activation hooks are empty. Required project-context.md is absent; repository search found no replacement. Ground the specification in current source and existing docs instead.
- (assumption) Deliver docs/rendering-library.md and docs/rendering-library.html following the existing expression-reference pair; link both from the root and library READMEs. No hosting or designer UI integration is inferred.
- (assumption) All APIs means exported declarations, methods and fields in the importable folio, fonts and wasm packages; distinguish core rendering from authoring/browser integration. Internal packages and executable commands are excluded from the Go API reference.
- (capability) CAP-1: Developers can install the rendering library through Go modules; success is a documented fresh-module setup with correct import paths, prerequisites, reproducibility caveat and a verified resolvable dependency version.
- (capability) CAP-2: Developers can create their first PDF and choose bytes or writer output; success is complete executable examples with a supplied valid template, data, params, fonts and error/diagnostic handling.
- (capability) CAP-3: Developers can understand inputs and diagnose failures; success covers raw JSON Data versus Params, shipped/custom/embedded fonts, validation, stable diagnostic codes, errors.As, writer failure and determinism limits.
- (capability) CAP-4: Developers can find every available exported API; success is zero omissions against a fresh export census, with signatures, fields, units, parameter/return semantics, mutation behavior, errors and applicable caveats.
- (capability) CAP-5: Developers can discover and navigate the reference page; success is working README links, section anchors, usable narrow-screen code blocks and equivalent Markdown/HTML content.
- (constraint) Use current implementation over stale README examples: RenderTo returns ([]Diagnostic, error), Render returns (Result, error), and Validate accepts raw template bytes; RenderTo buffers the whole PDF and can partially write before writer failure.
- (constraint) Installation derives module path and prerequisites from folio-go/go.mod: github.com/panitw/folio/folio-go, Go 1.25.0 minimum and repository toolchain go1.26.0. Consumer toolchains are controlled by their main module. Never claim an unpublished v0.1.0 release exists.
- (note) At local commit 3890ba8, Version is 0.0.0-dev and no folio-go/* tags exist locally; remote version availability is not verified by this specification run.
- (constraint) Use public imports only in runnable examples. SnapToGrid exposes internal geom.Length; disclose the constraint on externally naming its type rather than importing internal packages or redesigning the API.
- (decision) Companion documentation-content.md holds page structure and acceptance checks; api-inventory.md holds comment-free exported declarations derived via go doc -all for folio, fonts and wasm from current source. Reconcile this inventory when implementing.
- (constraint) Preserve current font, line-breaking, locale and reproducibility limitations; do not claim concurrency safety, streaming, stable release status or behavior unsupported by implementation/tests.
- (decision) Non-goals: changing rendering behavior or public API, cutting a release, hosting a docs site, replacing the expression/template language references, or expanding CLI documentation.
- (decision) Validation for implementation: run first-PDF and writer examples from an external consumer module against the selected dependency revision, check a valid nonempty PDF and equal bytes, verify diagnostics examples and export coverage, and inspect HTML navigation/responsiveness.
- (note) Wrapper-only content: slash-command invocation is execution metadata; no substantive user requirement was dropped.
- (direction) User requires opening the documentation from the top menu of Folio Designer.
- (decision) Supersedes initial exclusion of designer UI integration: CAP-5 now requires a top-menu Rendering library documentation action. Bundle/serve the page with the designer build, preserve unsaved editing state, support mouse and keyboard, and verify production-base-path/offline behavior. Other designer UI changes remain excluded.
- (assumption) Open documentation in a separate tab to preserve editor state; place an accessible labeled action in the existing top document bar/menu. Existing App.tsx contains a document-bar header rather than an established Help menu.
- (constraint) Reuse existing documentation visual conventions without copying its remote Google Fonts dependency into the offline designer; ship local or system fonts and include the documentation in applicable offline assets.
- (event) Coherence validation passed: five stable unique capabilities each have intent/success; concrete constraints, explicit non-goals and demonstrable success; menu access is included and obsolete exclusion superseded. Presentation and package-scope assumptions remain explicit.
- (event) Preservation validation passed: library overview, Go module installation, usage, all exported APIs and subsequent designer top-menu access are represented across SPEC and companions. Source-derived caveats and API declarations are retained. Only slash-command wrapper metadata was dropped.
- (event) Spec finalized with three derived artifacts and append-only memory. This run did not implement the page or execute its future acceptance tests.
- (direction) User invokes /bmad-spec: update this spec to reflect the latest features.
- (note) At HEAD 15871c7 the documentation page is still unimplemented (no docs/rendering-library.*, no README or designer links). Since 3890ba8 shipped: comparison/arithmetic/ternary formulas and true/false/null literals, exact-decimal numbers in text, proportional table widths, table frame/rules/minHeight, multi-line column labels, per-column header alignment, table cell padding, Code 128 barcode and QR code elements, content-band section break with Anchor, designed pages with Page Break, group movement. Supported .folio format is 4.1; go.mod and Version 0.0.0-dev unchanged; no folio-go/* tags.
- (constraint) API census refreshed from go doc -all at 15871c7: ApplyComponentCommand gains variadic fonts ...FontSet; new PreviewComponentMove, ComponentMove, AuthoredProperty[T], CanvasAuthoredProperties, CanvasBarcodeBar, CanvasBarcodePaint, CanvasQRCodePaint, CanvasQRCodeRect; wasm Engine.GroupMovePreview and GroupMoveResult; 11 new DiagCode constants (barcode x3, QR x3, pages, section break x3, table minHeight); new fields on CanvasComponent, CanvasProjection, CanvasTableColumn, TableColumnProjection, TableColumnsProjection. CanvasAuthoredProperties also names internal geom.Length, same disclosure rule as SnapToGrid.
- (constraint) ParseTemplate/LoadTemplate now refuse a table minHeight taller than the content window and an invalid section break; PAGES_INVALID and SECTION_BREAK_INVALID errors carry the band/page field path in Diagnostic.DataPath rather than an element id. The guide must document load-time refusal separately from render-time warnings.
- (capability) CAP-6: Developers can predict how template features added to the format affect rendered output and failures; success is a verified minimal template per feature (formulas in visibility/text, barcode and QR code, anchored and unanchored section break, multi-page Page Break on/off, table frame/rules/minHeight) that renders with the documented placement, and each feature's load errors and render warnings are listed by stable code.
- (decision) Guide summarises render-affecting template features and their minimum format versions, and links docs/expression-reference.md for formula syntax instead of duplicating it; a full .folio field reference stays out of scope. The authoring command catalog in api-inventory.md expands to the current command kinds (addPage, deletePage, setPageBreak, moveComponents, section-break and table-rules commands).
- (constraint) Examples using newer features declare the format version they require (proportional widths 3.0, table rules/minHeight 3.1, column headerAlign 3.2, barcode/QR 4.0, section break or multiple pages 4.1); the 1.0 first-PDF example stays valid. Barcode/QR claims are limited to what fixtures verify (decode equality, module-size warnings).
- (assumption) The .folio format reference exists only at _bmad-output/specs/spec-folio/folio-format.md, not under docs/; the guide describes the feature keys it needs rather than linking a planning artifact.
- (question) Should the .folio format reference be published under docs/ so the rendering guide can link it, or is the guide's feature summary sufficient?
- (note) folio-go/README.md:242 still shows the stale single-return RenderTo call; the existing correction constraint still applies.
- (constraint) Verified against diag.go and version.go: empty-average code string is AGGREGATE_EMPTY_AVERAGE; a text expression that can return a number requires format 3.3 (TextNumberExpressionVersion, applied on save); table width allocation errors set DataPath table.width or column.<field>, code to be confirmed at implementation.
- (event) Coherence validation passed: six stable unique capabilities (CAP-6 added) each with intent/success; constraints bend examples, versions and claim scope; non-goals now exclude a full .folio field reference; success signal names feature-template rendering. One assumption and one open question added on the format reference location.
- (event) Preservation validation passed: every feature shipped since 3890ba8 that changes rendered output is mapped with version and codes in documentation-content.md; API census refreshed to 15871c7 with all new declarations, fields and command kinds; designer-only interactions (group selection, bulk properties, canvas controls) appear only as exported API surface. No stories.yaml exists to check.
- (event) Spec update finalized: SPEC.md, documentation-content.md and api-inventory.md re-derived. The documentation page remains unimplemented and its acceptance checks were not run.
- (direction) User resolves the format-reference question: publish the format reference under docs/ so the guide can link to it.
- (decision) Resolves the open question and supersedes the planning-artifact assumption: this work publishes docs/folio-format.md derived from _bmad-output/specs/spec-folio/folio-format.md, and the guide links its sections for field rules instead of describing keys in full. The non-goal narrows from any format reference to authoring new format rules or rewriting the reference. CAP-5 success now includes resolving guide links to the published reference.
- (constraint) The published format reference keeps every normative rule of the planning copy, is reconciled against internal/template/version.go SupportedVersion (4.1 at 15871c7) and current load/serialize behavior, and removes planning traceability labels (FR, AD, D-, Story, QA) and internal source-file pointers that mean nothing to an external reader. This work does not edit spec-folio/folio-format.md, which remains a spec-folio companion.
- (assumption) Publish docs/folio-format.md plus docs/folio-format.html, following the guide's Markdown/HTML pair, and ship the HTML with the designer's documentation assets so guide links resolve in production and offline.
- (question) Once published, which copy of the format reference is canonical for future format changes: docs/folio-format.md or _bmad-output/specs/spec-folio/folio-format.md?
- (event) Coherence validation passed: six capabilities unchanged in ID with CAP-5 success extended to format-reference links; new constraint bends publication content; narrowed non-goal still explicit; the replaced assumption and new open question are recorded.
- (event) Preservation validation passed: the user's publish-under-docs direction lands in CAP-5, constraints, non-goals, assumptions and documentation-content.md deliverables, feature guidance and verification. The earlier format-reference question is resolved rather than dropped.
- (event) Spec update finalized: SPEC.md and documentation-content.md re-derived; api-inventory.md unchanged. Neither docs/folio-format.* nor the guide was created by this run.
- (direction) User: content in docs/ is the latest snapshot and the source of truth for documentation from now on.
- (decision) Resolves the canonical-copy question: docs/ is canonical for user-facing documentation (rendering guide, format reference, expression reference). Pages published by this work reflect the implementation at the delivery revision; later documentation changes land in docs/. After publication, _bmad-output/specs/spec-folio/folio-format.md is a historical planning snapshot and is not edited by this work.
- (note) spec-folio still lists ./folio-format.md as a companion; repointing it to docs/folio-format.md is a spec-folio update outside this spec, offered to the user rather than applied.
- (event) Coherence validation passed: capabilities unchanged; the docs/ source-of-truth rule is a constraint that bends where content and future edits land; no open questions remain.
- (event) Preservation validation passed: the user's direction lands in SPEC.md constraints and documentation-content.md format-reference rules; the prior open question is resolved, not dropped.
- (event) Spec update finalized: SPEC.md and documentation-content.md re-derived; api-inventory.md unchanged; no documentation pages created.
