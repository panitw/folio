---
topic: Startup dialog: pick Blank or a bundled example template (Invoice, Bank Statement, Legal Contract, Electricity Bill) with sample data previewable immediately
updated: 2026-09-15T20:14
---

- (direction) User: new users get a startup dialog offering Blank or example templates with made-up sample data previewable right away; examples: Invoice, Bank Statement, Legal Contract, Electricity Bill. Also design the UX/UI with Claude Design.
- (decision) Slug startup-templates (derived). Express distill; ambiguities settled by direct questions.
- (note) Brownfield: designer boots straight onto bundled starter.folio (blankBytes); toolbar Start blank discards current doc without a dirty guard; no per-user persistence; sample data is a separate JSON never stored in .folio, accepted via acceptSampleData; Preview renders stand-in data when none loaded; only starter.folio is bundled (build-wasm.mjs, offline-assets); modals are hand-built role=dialog React sections; no chart element.
- (decision) Dialog shows on every launch and is reopened by the toolbar's blank action (becomes New...); no remembered dismissal, so no new per-user persistence (user choice).
- (decision) Choosing an example loads its .folio plus its bundled sample JSON as if both were opened, then opens Preview (user choice).
- (decision) Dialog thumbnails are static images produced from real engine output at build time, so the dialog opens instantly and offline (user choice).
- (decision) Example data is fictional, English, neutral currency/formatting (user choice).
- (capability) CAP-1: on launch the author picks Blank or one of four examples from a dialog with page thumbnails; Blank yields today's starter document.
- (capability) CAP-2: picking an example loads its template and its sample data together and shows Preview rendered from that sample, not stand-in data.
- (capability) CAP-3: the dialog is reachable again from the document bar (New...), cancellable there; closing it at launch means Blank.
- (capability) CAP-4: four bundled examples (Invoice, Bank Statement, Legal Contract, Electricity Bill) with fictional English data, each exercising real engine features; catalogued in example-templates.md.
- (capability) CAP-5: an opened example is an ordinary unsaved, unnamed-target document; Save prompts for a location and edits never alter the bundled example.
- (constraint) Offline: examples, sample JSON and thumbnails ship in the verified offline bundle like starter.folio; nothing fetched.
- (constraint) Sample data stays a separate JSON, never embedded in the .folio (format rule).
- (constraint) Examples use only shipped element types and fonts; each must load and render with zero diagnostics, enforced at build so an example cannot rot.
- (constraint) Thumbnails derive from the engine's own render of each example at build time; a hand-drawn thumbnail is not allowed.
- (constraint) No per-user persistence introduced (no localStorage/IndexedDB for dialog state).
- (constraint) Dialog follows DESIGN.md: square corners, sheet shadow over scrim, cyan for selection, amber only for data, no emoji.
- (assumption) Reopening via New... over unsaved changes discards them like today's Start blank; whether to warn is an open question.
- (assumption) An opened example's title is the example name; no file target until Save.
- (question) Should New... warn or block when the current document has unsaved changes?
- (question) Can the author export/save the example's sample JSON to keep previewing after reload?
- (question) Should the dialog also offer Open existing file, or does Open stay only in the document bar?
- (decision) UX mockups drafted as a Claude Design canvas in planning-artifacts/ux-designs/ux-startup-templates-2026-09-15; adopted as UX reference.
- (note) Non-goals set: no user template library, no remote templates, no don't-show-again, no Thai/localized examples, no charts, no guided tour.
- (event) Self-validate pass 1 (coherence): PASS. 5 caps each with intent+success; constraints rule out network, embedded sample, hand-drawn thumbnails, persistence; 5 non-goals; success signal demonstrable.
- (event) Self-validate pass 2 (preservation): PASS. Startup dialog, Blank, 4 named examples, immediate preview with sample data, made-up data, UX design all landed. Wrapper-only dropped: 'As a new user' story framing.
- (event) Spec rendered (5 caps, companion example-templates.md). UX canvas published: https://claude.ai/artifact/HQmtoP29dQpYexk4mVjbjE (working files in ux-startup-templates-2026-09-15/mockups).
- (direction) User answered open questions: (1) warn and ask on unsaved changes; (2) author can save sample data; (3) dialog offers Open existing file.
- (decision) Supersedes assumption that New... silently discards: leaving a document with unsaved changes through the dialog asks first; Keep editing returns to the dialog with nothing lost, Discard proceeds (user choice).
- (capability) CAP-6: before the dialog replaces a document with unsaved changes (Blank, example, or Open file), the author is warned and must confirm.
- (capability) CAP-7: the author saves the currently loaded sample JSON to a local file, so an example's sample survives a reload and can be reopened as sample data.
- (capability) CAP-8: the dialog offers Open existing file, using the same local file open path as the document bar.
- (assumption) Confirmation offers Keep editing and Discard only, no Save-first; saved bytes are exactly the loaded sample JSON; save sample action sits with the existing sample data controls.
- (event) Open questions resolved; spec re-derived to 8 caps; mockups updated (Open existing file in footer, inline unsaved-changes confirmation).
- (event) Re-validate after update: pass 1 PASS (8 caps with intent+success, open questions empty); pass 2 PASS (all three user answers landed as CAP-6..8).
- (direction) User requested story breakdown; accepted 5-story split: pipeline+Invoice, other three examples, startup dialog, New/Open/confirm, Save sample data.
- (decision) spec_checkpoint true on all five stories; done_checkpoint only on the last story (pause at the end); no invoke_dev_with notes given (user choice).
- (decision) Stories 1-5 agreed, ids plain integers matching repo precedent: 1 pipeline+Invoice, 2 other examples, 3 startup dialog, 4 New/Open/confirm, 5 save sample data.
- (event) stories.yaml validity check: PASS (5 entries, required fields present, quoted unique prefix-free ids, no status field); all 8 caps covered.
- (direction) User: update example-templates.md to match what stories 1-2 shipped.
- (decision) Contract signatories are partyA.signatory / partyB.signatory {name, role}, not signatories[]: expressions have no index syntax to feed two fixed signature blocks (story 2).
- (decision) Contract clauses render as a clauses[]{no, heading, body} table (number column + heading/body column) with signatures below a section break; a text element cannot push later content (AD-24). Clause numbers come from data; headings share the body style.
- (decision) Sample shapes recorded as shipped: invoice adds invoice.reference, seller email/taxId, customer contact, totals.taxRate; statement adds account address/city, balances; bill adds account tariff/paymentRef, readings[], arrears.
- (decision) Shipped page counts pinned by the Go examples test: invoice 1, bank statement 2, legal contract 2, electricity bill 1; companion states them.
- (decision) Engine limits every example works within: one style per table column, no row index, no chart element, Code 128 barcode ASCII only; bold requires the bold face declared in the font chain.
- (event) Companion update validated. Pass 1 (coherence): PASS; SPEC.md capabilities unchanged, example-templates.md now matches shipped templates and samples. Pass 2 (preservation): PASS; every shipped shape, page count and engine limit landed. stories.yaml descriptions still match (story 2 'remaining three examples' holds).
