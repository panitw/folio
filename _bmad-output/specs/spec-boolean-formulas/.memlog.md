---
topic: Boolean formulas
updated: 2026-09-12T12:03
---

- (direction) User requests /bmad-spec for formulas in Visibility and other boolean-value fields: >, <, >=, <=, !=; +, -, *, /, %; x ? y : z; x ? (a ? b : c) : z. The motivating rejected input is loanAmount > 20000.
- (decision) Headless express spec creation; derive slug boolean-formulas from the request. Deliver specification artifacts, not implementation code.
- (note) Customization/config resolved; no prepend/append/completion actions. Configured persistent file project-context.md is absent and rg found no replacement. Ground brownfield claims in inspected source instead.
- (capability) CAP-1: Authors compare values in boolean formulas; success covers all five requested comparison operators, thresholds and type failures.
- (capability) CAP-2: Authors combine basic arithmetic with comparisons; success covers all five math operators, precedence, parentheses, decimal arithmetic, invalid operands and zero divisors.
- (capability) CAP-3: Authors choose values with simple and nested ternaries; success covers both requested forms, right association, strict conditions and lazy branch evaluation.
- (capability) CAP-4: Authors use one formula contract in boolean expression contexts; success covers Visibility, if() conditions and ternary conditions, preserves strict final boolean/null handling and existing fixed controls unless explicitly expanded.
- (capability) CAP-5: Authors receive actionable errors and retain formulas through editing and file round trips; success covers commit, load/save, undo/redo, worker execution and rendering with two datasets.
- (constraint) Extend shared Go expression parsing/checking/evaluation without JS eval or a separate designer grammar; preserve existing eight named functions, data/params scopes, lazy if(), null-as-false condition semantics, Decimal arithmetic and document-scope visibility.
- (constraint) Old valid documents remain valid with equivalent behavior; explicitly supersede the no-operator grammar restriction for the requested operators. New expression nodes must be handled by parameter discovery, stand-in generation, footer derivation and every AST consumer.
- (assumption) A1: Scope defaults to existing expression condition inputs; fixed boolean style controls such as Bold/Italic remain fixed pending the user's scope answer. Current code forbids data-driven formatting, so expanding these requires an explicit contract/schema change.
- (assumption) A2: Only requested comparison operators are included; ==, ===, !==, &&, ||, !, assignment and general scripting are out of scope.
- (assumption) A3: Ordering is numeric Decimal only; != compares same-kind scalar numbers, strings and booleans, with numeric scale-insensitive comparison; null != null is false, null != a non-null scalar is true; non-null mixed kinds and collections error.
- (assumption) A4: Parentheses and unary +/- are included to support ordinary math. Precedence is grouping/calls, unary, multiplicative, additive, ordering, !=, ternary; arithmetic associates left and ternary right; reject chained comparisons unless explicitly composed into valid typed operands.
- (assumption) A5: Preserve existing literal vocabulary and field names; ternary branches may use boolean data paths. Adding true/false/null literal keywords is deferred to avoid silently changing existing bare paths with those names.
- (assumption) A6: Ternary returns the selected branch's value without coercion; numeric/string values may be intermediate results, but a boolean-consuming field still requires boolean or existing null-as-false behavior. Parse/static-check both branches; resolve/evaluate only the selected branch.
- (assumption) A7: +, -, * and % use exact bounded decimal arithmetic; modulo uses a truncated-toward-zero quotient and dividend sign. Zero divisors, overflow and exceeded bounds are located errors, not NaN/infinity or implicit false.
- (question) Q1: Confirm scope: existing condition inputs only, or also formula-backed Bold/Italic and other fixed boolean properties? Async scope question sent.
- (question) Q2: What precision should division use for non-terminating results? Proposed default is max(operand decimal scales, 0) + 4 fractional places, round-half-to-even, modeled on existing avg; unconfirmed and must be resolved before arithmetic implementation is finalized.
- (constraint) Bound source length, AST depth and evaluation work including parentheses, unary and ternary nesting; distinguish field syntax/static failures from data-dependent evaluation failures and preserve command atomicity.
- (decision) Produce SPEC.md plus formula-language.md and brownfield.md. No stories.yaml requested or generated; existing spec-folio is not rewritten.
- (note) Derived companion details from inspected paths cited in brownfield.md: parser/static checker/evaluator, decimal/reduce, binding/visibility validation, property commands, parameter discovery, stand-in AST walking and footer derivation. Shared expression containers retain existing output-type rules and wrappers.
- (event) Coherence pass: PASS with explicit assumptions A1-A7 and unresolved Q1/Q2. Five unique capabilities each carry intent/success; constraints bend design, non-goals are explicit, acceptance is demonstrable, and detail lives in companions. Specification complete; arithmetic precision and fixed-property expansion are not implementation-ready decisions.
- (event) Preservation pass: PASS. Visibility and other boolean consumers map to CAP-4/Q1; > < >= <= != map to CAP-1; + - * / % map to CAP-2/Q2; simple and nested ternary map to CAP-3. Original rejected formula maps to Why and success signal; all have language acceptance rows. Wrapper-only content: slash-command ceremony and HTML whitespace entity omitted from product requirements.
- (event) Specification finalized with SPEC.md, formula-language.md and brownfield.md. Async Q1 remains unanswered; no implementation code changed.
- (decision) A5 confirmed by the user during review: true, false, and null remain data-field names; no boolean/null literal keywords are introduced. This explicit language choice remains in force independently of the later compatibility waiver.
- (direction) User confirms no release has reached external users and authorizes the formula extension without backward-compatibility requirements. Supersedes the earlier blanket promise that all previously valid documents remain valid with equivalent behavior.
- (decision) Formula syntax joins the current unreleased .folio 2.0 format; no separate major version, migration layer, or legacy expression mode is needed for this extension. Formula-bearing serialized content requires 2.0 under the existing content-based version rule; save does not lower a declared version.
- (decision) Apply the draft static-validation contract consistently to if() and ternary branches without legacy-checker exceptions: statically provable misuse is rejected during validation even in an unselected branch, while runtime path resolution, type failures, and caveats arise only on evaluated paths. This is the implementation consequence of the compatibility waiver, not a change to runtime branch laziness.
- (constraint) Retain the eight named functions, data and parameter scopes, strict boolean/null conditions, decimal representation, document-scope visibility, and fixed-style scope pending Q1. The pre-release compatibility waiver does not add syntax, literals, data scopes, or dynamic styling beyond this request.
- (note) Review follow-up resolves format-version choice and the legacy static-check compatibility concern; the report keeps the original findings with their resolution. Q1 property scope and Q2 division precision remain unanswered; other review findings remain open.
- (event) Coherence pass after review clarification: PASS for this update. CAP-1 through CAP-5 retain intent/success and stable IDs; release 2.0 and confirmed A5 are constraints, uniform static checking has acceptance examples, and Q1/Q2 remain open. Other review findings remain outstanding; this is not an implementation-readiness verdict.
- (event) Preservation pass after review clarification: PASS. User confirmation of no external release supersedes the blanket backward-compatibility promise; formulas join unreleased 2.0 without migrations or a legacy checker. Earlier explicit A5 is retained, runtime laziness and other active constraints remain, and no property-scope or division-precision answer is inferred. SPEC.md and both companions re-derived; review resolutions synchronized.
- (decision) A5 revised by explicit user instruction: make true, false, and null literal values. This supersedes both earlier decisions to preserve these bare words as data-field names. Boolean literals produce boolean values and null produces the explicit null value, with no data lookup.
- (assumption) A8: Literal keywords are case-sensitive, lowercase whole identifiers. trueFlag, falseValue, and nullable remain paths; quoted words remain strings. Keywords are reserved as bare expressions and path roots, while segments after a dot remain field names (record.true, params.null); true() and true.field are invalid. No new escaping or data namespace is introduced.
- (constraint) Boolean/null literals are valid in Visibility, if() conditions and ternary conditions/branches. true means visible or then; false and null mean hidden or else. A literal null has the same value semantics as a resolved JSON null; missing paths remain errors, and numeric/string conditions remain invalid.
- (constraint) Preserve inequality's declared scalar/null rules for literal operands. Reject boolean/null operands in arithmetic and numeric ordering, and in aggregate path-only or format-pattern-only argument slots. Replace the old condition-slot ban on all literals with kind-aware validation; keep independent restrictions for aggregate paths and string patterns.
- (capability) CAP-4 updated success: Visibility, if() and ternaries share boolean/null semantics, including the new true, false and null literals. Existing numeric/string-result rejection and fixed-property scope pending Q1 still apply.
- (constraint) Parameter discovery and stand-in generation treat new literals as leaves with no data references; constant true/false/null conditions work with an empty supplied data object and in no-sample-data preview. Persist literal-containing formulas through commit, undo/redo, save/reload and native/worker rendering under unreleased format 2.0.
- (note) Update the review's A5 decision to the latest owner choice and re-derive SPEC.md plus both companions. Q1 property scope and Q2 division precision remain open; no implementation or story dispatch is requested.
- (event) Coherence pass after literal revision: PASS for this update. Five capabilities retain stable IDs and intent/success; confirmed A5 now permits all three literals, A8 exposes inferred keyword rules, and condition/argument/text-consumer distinctions have acceptance cases. Q1/Q2 and unrelated review gaps remain open; no full implementation-readiness verdict is claimed.
- (event) Preservation pass after literal revision: PASS. The latest explicit user decision supersedes all earlier no-literal directions in the append-only log. SPEC.md, formula-language.md, brownfield.md and the review now agree on true/false/null literals, while retaining unreleased 2.0, strict typing, missing-path errors, runtime laziness, existing consumer output rules and pending property/precision decisions. Production code and story dispatch were not changed.
- (decision) Q1 resolved by the user: only existing condition inputs for now. A1 is confirmed. Visibility, if() and ternary conditions are in scope; Bold/Italic and all other fixed boolean properties remain fixed, with no formula-backed styling or new property schema/editor controls.
- (decision) Q2 resolved by the user: accept the proposed division rule. For operand Decimals with exponents ea and eb, S=max(-ea,-eb,0)+4 fractional places; round the mathematical quotient once to S using round-half-to-even. This is the accepted numeric contract, not a proposal.
- (constraint) Division uses each evaluated operand's retained Decimal scale; apply the rule at each division operator, including terminating quotients, and retain result exponent -S without trimming zeros to alter later precision or evade bounds. A rounded nonzero quotient may become zero; use the existing unsigned zero coefficient with exponent -S. Zero divisors and out-of-bound result coefficient/exponent or resource use remain located errors.
- (note) Q2 acceptance includes 1/3=0.3333, 2/3=0.6667, 1.00/3=0.333333, 1/8=0.1250, half-even ties 1/32=0.0312 and 3/32=0.0938, their negative counterparts, exponent inputs, tiny positive/negative results rounded to scale-preserving zero, and comparison outcomes affected by retained operand scale. Decimal fixture calculations verified independently of engine implementation.
- (note) Remove Q1/Q2 from Open Questions and all active pending/proposed wording in the derived spec and review. Retain confirmed literals and unreleased 2.0 decisions, stable CAP IDs, and existing shared text/value expression behavior. Other review findings remain open; closing Q1/Q2 does not declare the whole spec implementation-ready.
- (event) Preserved user content: Q1 selects existing condition inputs only; Q2 accepts the named precision proposal. Wrapper-only content: trailing HTML whitespace entity omitted.
- (event) Coherence pass after Q1/Q2 answers: PASS for this update. Five stable capabilities retain intent/success; Q1/A1 and Q2 are confirmed constraints, both former open questions are removed, and the companions/review carry no stale pending wording. Verified thirteen signed/exact/repeating/tie/exponent/tiny division fixtures plus per-division scale, comparison and coefficient-bound expectations. Remaining review findings are not declared resolved.
- (event) Preservation pass after Q1/Q2 answers: PASS. Existing condition inputs only maps to the scope constraint, fixed-property non-goal and acceptance row; the accepted precision proposal maps to the division algorithm, scale retention and exact acceptance cases. Confirmed literal and unreleased-2.0 decisions remain, inferred defaults remain labeled, and the historical log is append-only. SPEC.md, both companions and review are synchronized; production code is unchanged.
