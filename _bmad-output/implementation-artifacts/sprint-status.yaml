# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed

# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created, ready for development
#   - in-progress: Developer actively working on implementation
#   - review: Implementation complete, ready for review
#   - done: Story completed

# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed

# Action Item Status:
#   - open: Committed during a retrospective, not yet addressed
#   - in-progress: Actively being worked on
#   - done: Completed

# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when its first story starts (via build's sprint sync)
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates the next story after the previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)
# - Retrospective appends its action items to action_items; the status view surfaces open ones
generated: 08-23-2026 09:38
last_updated: 08-31-2026
project: folio
project_key: NOKEY
tracking_system: file-system
story_location: _bmad-output/implementation-artifacts
development_status:
  # ---------------------------------------------------------------------------
  # DO NOT run `sprint_plan.py generate` against this file without first
  # re-adding the four inserted stories below. Verified 2026-08-29: the script
  # regenerates development_status FROM epics.md and drops anything absent there.
  # 2-3a, 2-5a, 2-6a and 3-1a are all `done`, all have story files on disk, and
  # are all DELIBERATELY absent from epics.md — each story file states it in
  # bold ("This story does not come from epics.md ... epics.md is not amended by
  # this story and must not be"), per D-000.25, D-2.6.9 and D-3.1a.1. So the
  # omission is a RULING, not drift, and must not be "fixed" by amending
  # epics.md. `--set` cannot restore them either: it only retargets keys the
  # generated plan already contains. A bare `generate` run therefore destroys
  # the record of four completed stories. Reconcile by hand, as of this update.
  # ---------------------------------------------------------------------------
  epic-1: done
  1-1-a-minimal-pdf-reproducible-on-one-machine: done
  1-2-cross-target-byte-identity-proven-in-ci: done
  1-3-guardrails-that-fail-the-build: done
  1-4-load-validate-and-round-trip-a-folio-template: done
  1-5-embed-and-subset-a-font-byte-stably: done
  1-6-bind-scalar-json-values-into-text: done
  1-7-render-to-an-io-writer-with-runtime-parameters: done
  1-8-embed-and-render-an-image-from-the-template: done
  epic-1-retrospective: optional

  # epic-2 closed 2026-08-25 over one REQUIRED red (TestCorpusMeetsP6ExerciseFloors,
  # P6g 7<20) which D-000.17/D-2.1.14 mandate stay unmet. See epic-2-boundary-gate.md.
  epic-2: done
  2-1-thai-break-opportunity-spike: done
  2-2-the-shipped-font-set-and-its-fallback-chain: done
  2-3-shape-latin-thai-and-cjk-text: done
  # 2-3a inserted between 2.3 and 2.4 per D-000.25 (vendor-boundary audit +
  # type-aware AD-23 guard). Must land before 2.4 adds vendor call sites.
  2-3a-audit-the-vendor-boundary: done
  2-4-break-and-measure-lines-in-all-three-scripts: done
  2-5-compose-a-page-from-three-bands: done
  # 2-5a inserted before 2.6 to discharge DW-15 (first baseline placed from font
  # size vs inter-baseline spacing from hhea ascent). Must land BEFORE the Thai
  # reading sign-off is recorded: it re-records shaped-text/expected.pdf, whose
  # digest that sign-off binds to (D-000.26 refined).
  2-5a-align-first-baseline-with-the-leading-model: done
  2-6-render-multi-page-documents-with-headers-and-footers-on-ever: done
  # 2-6a inserted between 2.6 and 2.7 per D-2.6.9 (behavioural sweep of the shared
  # PDF-reading test helpers for single-page-shape assumptions). Must land BEFORE
  # 2.7, which adds a run on every page and lands directly on those helpers.
  2-6a-sweep-single-page-assumptions-from-the-shared-pdf-readers: done
  2-7-render-page-x-of-y: done
  2-8-clip-overflowing-content-and-say-so: done
  epic-2-retrospective: optional

  # Boundary gate passed 2026-08-26: matrix green on 4 targets x 10 documents,
  # 4 blockers closed, 11 items carried forward. See epic-3-boundary-gate.md.
  epic-3: done
  3-1-bind-a-repeating-region-to-a-collection-with-an-explicit-row: done
  # 3-1a inserted between 3.1 and 3.2 per D-3.1a.1, on D-000.25's precedent (which
  # inserted 2.3a for this same guard). AD-23's float ban is keyed on float32/float64
  # and cannot see math/big.Float. Must land BEFORE 3.2 creates internal/expr.
  3-1a-guard-ad-23-against-named-binary-float-types: done
  3-2-evaluate-the-expression-language: done
  3-3-aggregate-over-a-collection: done
  3-4-format-dates-and-numbers-by-declared-locale: done
  3-5-hide-a-component-conditionally: done
  3-6-fail-with-located-actionable-diagnostics: done
  3-7-validate-a-template-and-render-it-from-the-command-line: done
  epic-3-retrospective: optional

  epic-4: done
  4-1-lay-out-a-table-s-columns-header-and-cell-chrome: done
  4-2-generate-rows-from-a-bound-collection: done
  4-3-break-a-table-across-pages-without-splitting-a-row: done
  4-4-repeat-the-table-header-on-every-continuation-page: done
  4-5-render-footer-aggregates-without-orphaning-them: done
  4-6-handle-a-row-taller-than-the-page: done
  4-7-the-golden-report-renders-and-hashes-match: done
  4-8-alternating-row-styling: done
  epic-4-retrospective: optional

  # Boundary gate passed 2026-08-28: 14/14 real Playwright scenarios, offline
  # release verification, and the 4-target x 15-document matrix are green.
  # See epic-5-boundary-gate.md.
  # Reopened 2026-08-29: Story 5.13 completes FR4/FR5 for the Image component, which
  # Stories 5.7/5.8 left without any way to choose the picture. The gate above stands as
  # baseline-anchored evidence for 5.1-5.12; a second Epic 5 boundary is owed once 5.13 lands.
  # Second boundary gate passed 2026-08-29 at 6c79d29: 23/23 Playwright scenarios,
  # the 4-target x 16-document matrix (incl. Story 5.13's component-asset-import),
  # offline release verification, and the post-Epic-6-gate delta. See
  # epic-5-boundary-gate-2.md. DW-23 filed for a pre-existing lint gofmt break.
  epic-5: done
  5-1-the-design-system-and-application-shell: done
  5-2-the-engine-worker-and-command-channel: done
  5-3-work-offline: done
  5-4-an-honest-first-run-load-screen: done
  5-5-open-and-save-folio-files-locally: done
  5-6-the-canvas-page-bands-grid-and-page-setup: done
  5-7-place-and-manipulate-the-five-components: done
  5-8-edit-component-properties: done
  5-9-a-canvas-the-browser-never-measures: done
  5-10-preview-the-exact-production-document: done
  5-11-never-show-a-stale-preview: done
  5-12-diagnostics-that-locate-and-an-interface-that-can-be-driven: done
  5-13-import-an-image-and-set-it-on-an-image-component: done
  epic-5-retrospective: optional

  epic-6: done
  6-1-load-sample-data-and-browse-its-paths: done
  6-2-bind-a-component-by-picking-a-path: done
  6-3-supply-parameters-for-preview: done
  6-4-configure-table-columns-as-a-matrix: done
  6-5-bind-columns-in-row-scope-and-configure-footer-aggregates: done
  6-6-present-a-failed-render-honestly: done
  6-7-the-round-trip-closes: done
  epic-6-retrospective: optional

  # Epic 7 added 2026-08-29 (post-MVP). Long-form body text + a multi-page authoring
  # canvas, for legal-document templates. Scoped in conversation against the alternative
  # of per-page layouts, which was REJECTED: paginate.go's window model (D-2.6.1, rule 1
  # — "more windows, never rearranged furniture") and the page-count-N/statement-N golden
  # corpus both rest on one unbounded content column whose page count comes from DATA.
  # Per-page layout implies content that never reflows, i.e. silently clipped clause text.
  # Epic 7 changes the AUTHORING SURFACE and TYPOGRAPHY only; pagination is a constraint
  # on it, not a target of it. Every field it adds is optional and absent-by-default, so
  # the existing corpus must hash identically.
  epic-7: in-progress
  7-1-break-a-line-where-the-author-typed-a-break: done
  7-2-set-the-space-between-a-paragraph-s-lines: done
  7-3-justify-a-paragraph-s-edges: done
  # 7.4 owns DW-25 and DW-29 as acceptance criteria, not deferrals. See
  # epic-7-8-decision-log.md D-7.4.1..D-7.4.5.
  7-4-author-body-text-in-the-designer: done
  7-5-the-content-column-runs-past-the-first-page: done
  7-6-the-canvas-draws-every-page-the-document-will-produce: in-progress
  7-7-keep-a-signature-block-together-across-a-page-break: backlog
  # 7.8 added 2026-08-30 by Story 7.4's plan gate, which routed DW-29 out.
  7-8-refuse-a-justified-table-at-load-in-the-author-s-own-terms: backlog
  epic-7-retrospective: optional

  # Epic 8 added 2026-08-30 (post-MVP). Fonts an author can choose and a file can carry,
  # from SPEC-fonts (_bmad-output/specs/spec-fonts). Scoped in conversation against the
  # alternative of a zip-of-folders .folio, which was REJECTED: the format exists so a
  # person or an agent can edit a template without the designer (FR12) and so a
  # hand-written template renders (S9), and a container puts entry order, timestamps and
  # compression inside the byte-identity regime. Font bytes therefore ride the existing
  # content-addressed assets map. Bold/italic are NOT in scope — they need a face
  # inventory decision SPEC-fonts leaves open. Every field is optional and
  # absent-by-default, so the existing corpus must hash identically.
  epic-8: backlog
  8-1-the-document-s-font-chains-become-editable: backlog
  8-2-the-chain-editor-sits-where-fonts-are-chosen: backlog
  8-3-a-font-travels-inside-the-template: backlog
  8-4-the-engine-renders-from-an-embedded-face: backlog
  8-5-a-curated-catalogue-ships-with-the-designer: backlog
  8-6-picking-a-family-puts-it-in-the-file: backlog
  epic-8-retrospective: optional

  # Epic 9 added 2026-08-30 (post-MVP). Reported from the designer: BOX properties set on a
  # text element (and on a Rectangle) have no effect. Traced — render.go references neither
  # Background nor Border; the only producer of pagemodel.Rect on the whole render path is
  # tableRects (render.go:1866), fed from the TABLE element's own style (table_render.go:293),
  # so element background/border are inert on text/image/rect/line, and rect/line never reach
  # the page model at all (render_visibility.go's "LATENT, not broken"). No field, property or
  # format version is added: every value this epic starts consuming is one the format already
  # carries and validates, so a document declaring no element background or border must hash
  # identically and the golden corpus stays a witness.
  # CORRECTED 2026-08-30 by Story 15.1 (D-15.1.1, D-R7.5): that claim is TRUE of the box paint
  # and FALSE AS SHIPPED. The same commit also created folio-go/text_alignment.go and wired
  # style.align into the emitter, which moved ALL FOUR statement goldens by +4 bytes per page.
  # The box paint moved nothing; the alignment did. See the second correction below.
  # Both stories implemented 2026-08-30 in the same session that filed the epic.
  # 9.1 also fixed a latent Epic 4 defect it surfaced: internal/pdf/rectdoc.go's
  # appendEdge emitted a stroked edge PREFIX ("m x1 y1 l x2 y2") in a postfix
  # format, so every four-edge border drew as diagonals between opposite corners.
  # No golden moved ON THAT FIX — no document in the corpus renders a stroked edge, and
  # Story 15.1's operator census confirmed the path is never reached rather than assuming it —
  # and the order is now pinned by TestAppendRectContentStreamEdgeOperandsArePostfix.
  # 9.1 ALSO shipped folio-go/text_alignment.go with no story file, no AC and no FR, and that
  # is what moved the corpus (D-15.1.1). Its two ROUNDED branches -- align:"center" and
  # valign:"middle", the file's only geom.ScaleRound site -- are declared by NO fixture and so
  # have zero golden coverage: DW-24.
  # NOT yet reviewed or retrospected; story files are not written.
  epic-9: in-progress
  9-1-the-engine-paints-a-component-s-background-and-border: review
  9-2-line-and-rectangle-draw-in-the-designer-and-in-the-pdf: review
  epic-9-retrospective: optional

  # Epic 10 added 2026-08-30 (post-MVP). Reported from the designer: there is no way to set
  # a text element's colour. Traced — `style` carries no colour for text at all: `background`
  # is the box, and pagemodel.TextRun and internal/pdf/textdoc.go never had an ink, so every
  # document this engine ever produced is black. Adds ONE optional format field, style.color,
  # carried end to end (model, parse, serialize, the schema-derived placeholder fence, the
  # page model, the PDF text emitter, the table cascade, the property command, the projection,
  # the TYPOGRAPHY control and the canvas paint). Absent-by-default and emitting nothing when
  # absent, so the existing corpus hashes identically.
  # NOT yet reviewed or retrospected; the story file is not written.
  epic-10: in-progress
  10-1-text-prints-in-a-declared-colour: review
  epic-10-retrospective: optional

  # ---------------------------------------------------------------------------
  # SPRINT SEQUENCE for Epics 11-15, agreed 2026-08-30.
  #
  # This file has no sprint field -- sequence is recorded here, and `ready-for-dev`
  # is NOT used to encode it, because that status means "story file created" and no
  # story file exists for epics 7-15 yet. Everything below stays `backlog` until its
  # story file is written.
  #
  #   S1  15.1                 UNBLOCK. main is RED on the cross-target matrix; every
  #                            other epic lands on a baseline nobody has explained.
  #                            Doing this after building on top is strictly harder.
  #   S2  13.1, 13.2, 13.4     One module (folio-designer/src/preview/), highest value
  #                            per unit of work. 13.1 is the smallest useful story in
  #                            the whole backlog and the one an author needs today.
  #                            13.4 joins them (moved from S5, 2026-08-30): it is what
  #                            makes Preview reachable at all without a JSON file, so it
  #                            belongs with the stories that make Preview usable, not
  #                            with the evidence rail. It carries a SCOPE FENCE -- the
  #                            empty-value behaviour is preview-with-no-data only and
  #                            must never become Render's semantics; BINDING_PATH_ABSENT
  #                            stays a located Error (Story 3.6, S8).
  #   S3  15.2                 Make CI's red readable BEFORE the wider surface of
  #                            12/13/14 starts landing in it.
  #   S4  12.1-12.5, 14.1-14.5 Independent of each other; parallelisable. 12.4 and 14.1
  #                            each OPEN with a ruling/rule, not with code. 12.5 follows
  #                            12.1 within the sprint -- the drag commits through 12.1's
  #                            command. 14.5 can go any time; it depends on nothing.
  #                            14.6 (DATA tab fidelity) OPENS with a ruling too: the
  #                            mockup shows no connect control, so pick-binds-immediately
  #                            vs keep-the-explicit-commit is decided before it is built.
  #                            14.7 carries TWO rulings that are not fidelity questions:
  #                            Cancel/Apply implies a local uncommitted buffer (the second
  #                            document model AD-15 forbids), and the design's MILLIMETRES
  #                            contradict the points every other panel shows -- a
  #                            PRODUCT-WIDE unit decision, not a dialog's.
  #                            14.8 FOLLOWS 12.3: 12.3 makes the table's header/rows
  #                            authorable, 14.8 presents them. Do not build 14.8 first --
  #                            it would have to invent storage 12.3 already owns.
  #                            14.9 is the ONE story in Epic 14 with engine work: the
  #                            canvas projection carries only `tableBind` (one string),
  #                            so the canvas CANNOT draw column headers or a specimen
  #                            row today. It adds a PROJECTION field, never a format
  #                            field. 14.10 depends on it -- you cannot click a column
  #                            the canvas does not draw -- and on 14.6 for the DATA
  #                            panel that offers the row-scope fields.
  #                            Owner instruction (2026-08-30): column binding belongs in
  #                            the MAIN WINDOW; the table editor keeps structure.
  #   S5  13.3, 13.5           The preview evidence rail and the chrome around it: the
  #                            real build in Epic 13. 13.3 carries a RULING -- the
  #                            design prints "Matches native render / Identical bytes on
  #                            darwin/arm64, linux/arm64, linux/amd64" and the TAB CANNOT
  #                            KNOW THAT. Cross-target identity is a property of the
  #                            release proven by CI's matrix, not a live comparison, so
  #                            the wording is earned or reworded before it is drawn.
  #   S6  11.1-11.3            GATED on the owner's realize-vs-retire ruling (see below).
  #                            Sequenced last of the feature work because it is the only
  #                            epic that can move golden hashes, so it wants the S1
  #                            baseline settled underneath it.
  #   S7  15.3                 LAST BY CONSTRUCTION -- it freezes the public API, so it
  #                            follows whichever epics are ruled inside v0.1.0.
  #
  # Readiness gate: CONCERNS, not PASS. Three findings, none of which block sequencing:
  #   1. Epic 11 carries a forward dependency on a decision NOTHING RECORDS. SPEC-fonts
  #      records the question ("Do bold and italic get realized in this scope?"); no
  #      artifact records an answer. 11.1 must not enter a sprint before it is ruled.
  #   2. Epics 9 and 10 are `review` with no story files and no retrospective, and 15.1
  #      most likely attributes the moved goldens to one of them -- so the evidence 15.1
  #      needs is the evidence those two epics never wrote down.
  #      SETTLED 2026-08-30 by Story 15.1 (D-15.1.1): ALL FOUR statement goldens moved, not
  #      statement-1 alone, and EPIC 9 ALONE is implicated -- Epic 10 (304442f) moved nothing,
  #      because the text-ink bracket emits zero bytes when no colour is declared.
  #   3. Epic 8's prose still reads "Bold and italic are NOT in this epic ... a
  #      face-inventory decision SPEC-fonts leaves open". Epic 11 now owns it; that
  #      sentence should point there rather than dangle.
  #
  # Generator note (2026-08-30): `sprint_plan.py generate` was run against this file and
  # its output was DISCARDED, restoring this file from backup. It did exactly what the
  # header above warns of -- dropped 2-3a/2-5a/2-6a/3-1a -- and additionally stripped 87
  # of 100 inline comments, including that header. What was KEPT from the run is its key
  # derivation: seven story keys here were renamed by hand to the script's canonical form
  # (64-char truncation; an apostrophe becomes `-s-`, e.g. `a-table-s-header`), so a
  # future --dry-run reports those keys as in-sync rather than as renames.
  # ---------------------------------------------------------------------------
  # Epics 11-15 added 2026-08-30 (post-MVP). Scoped in conversation from a designer-UI
  # and release-readiness audit; every finding was reproduced against the running app or
  # the live CI before it was written up. Like Epics 7-10 these are DELIBERATELY authored
  # by hand rather than generated -- see the header warning above.
  #
  # 11 is BLOCKED on an owner ruling before 11.1 starts: realize bold/italic as shipped
  # faces, or retire the toggles. SPEC-fonts records the same question as open. The epic
  # specifies only the "realize" branch; the "retire" branch replaces 11.1-11.2 with one
  # removal story.
  epic-11: backlog
  11-1-the-shipped-families-gain-a-weighted-and-a-sloped-face: backlog
  11-2-the-engine-resolves-a-face-from-the-declared-weight-and-slop: backlog
  11-3-the-canvas-paints-the-weight-the-engine-resolved: backlog
  epic-11-retrospective: optional

  # 12.4 is a RULING story: the owner decides whether padding insets a non-table element
  # or is documented table-only, and the ruling is recorded BEFORE any code is written.
  epic-12: backlog
  12-1-the-page-header-and-page-footer-take-the-height-the-author-s: backlog
  12-2-the-document-declares-its-locale-and-its-utc-offset: backlog
  12-3-a-table-s-header-and-its-alternating-rows-are-authorable: backlog
  12-4-padding-is-honoured-outside-a-table-or-the-format-says-it-is: backlog
  12-5-a-band-boundary-is-dragged-on-the-canvas: backlog
  epic-12-retrospective: optional

  epic-13: backlog
  13-1-the-preview-keeps-the-pdf: backlog
  13-2-the-viewer-navigates-like-a-pdf-viewer: backlog
  13-3-the-preview-screen-is-the-evidence-screen: backlog
  13-4-preview-runs-without-sample-data-and-an-absent-value-is-empt: backlog
  13-5-the-chrome-tells-the-truth-about-the-preview: backlog
  epic-13-retrospective: optional

  # 14.1 writes the button rule FIRST; the sweep is its consequence, not its substance.
  epic-14: backlog
  14-1-one-button-vocabulary: backlog
  14-2-a-line-is-a-thickness-and-a-colour-a-rectangle-is-a-fill-and: backlog
  14-3-a-placed-component-is-the-selected-component: backlog
  14-4-the-panel-offers-no-control-the-engine-will-refuse: backlog
  14-5-the-product-wears-its-own-mark: backlog
  14-6-the-data-tab-is-the-binding-panel-the-design-drew: backlog
  14-7-the-table-editor-is-the-matrix-the-design-drew: backlog
  14-8-the-table-editor-carries-the-header-cell-and-border-sections: backlog
  14-9-the-canvas-draws-the-table-it-will-print: backlog
  14-10-a-table-column-is-bound-from-the-main-window: backlog
  epic-14-retrospective: optional

  # Epic 15 is the release blocker set, and 15.1 was URGENT rather than merely first.
  # AS FOUND (before 15.1 ran): main was RED on the cross-target matrix. ALL FOUR golden
  # statements had moved -- statement-1, -5, -20 and -50 -- each by exactly +4 bytes per page
  # (+4/+20/+80/+200); statement-1, the primary acceptance fixture, produced 114df1d6...
  # against a then-recorded ef58bbf6...  All four targets agreed throughout, so CAP-13/S2/S3
  # never lapsed; what had failed was that a golden moved unexplained, which C6 makes a defect
  # until proven intended.
  # AS IT STANDS NOW: 114df1d6... IS statement-1's recorded digest, and all four targets agree
  # on it and on the other three. The matrix is still red, but for an intended reason -- the
  # human sign-off is pending re-attestation. There is no longer an unexplained hash.
  # EPIC 9 ALONE is implicated (D-15.1.1): 791ed00 created folio-go/text_alignment.go and
  # wired style.align to the emitter, moving one Tm x-operand on the per-page footer element.
  # Epic 10 (304442f) moved nothing. The move is INTENDED (D-R7.6) and the goldens are
  # re-recorded; the human sign-off is re-attested, never patched.
  # 15.1 pulled forward as story zero of the Epic 7-8 run (D-R7.0): both epics guard
  # themselves with "the corpus hashes identically", unevaluable while it is red.
  # Ruling and attribution in epic-7-8-decision-log.md (D-R7.5, D-R7.6).
  epic-15: in-progress
  # 15.1 CLOSED 2026-08-30 at 36bb3f5: attribution D-15.1.1, four goldens re-recorded on the
  # owner's ruling (D-R7.6), and the human sign-off written by the owner themselves. Full record,
  # measured gates and deferrals in the story's ## Delivery Log; DW-24 open, owner Story 7.3.
  15-1-the-golden-report-s-moved-hash-is-explained: done
  15-2-ci-s-red-means-something: backlog
  15-3-folio-go-v0-1-0-is-cut: backlog
  epic-15-retrospective: optional
