# Third-party licence manifest

Generated by `lint/internal/manifest` (AC19, AD-26). Every module in the
resolved dependency graph of each of this repository's three Go modules —
`folio-go`, `hashmatrix` and `lint` — appears below with its resolved
licence, which module it serves, and whether that module ships to
consumers or exists only to build/test the repo. An unresolved or
unrecognised licence fails the build (AC19) rather than appearing here
silently; a forbidden licence (GPL, LGPL, AGPL, SSPL, or a commercial
EULA) fails the build uniformly across all three modules, with no
build-time-only relaxation (AC18, D-1.3.9).

**`lint`'s own dependency, if any, is covered by this same table below.
A checker covering its own dependency is correct, not a bootstrap
problem** (AC20, D-1.3.6, D-1.3.9) — it must not be "fixed" by excluding
lint's own module: AD-26 binds all, and a licence manifest that omits the
one module doing the checking is exactly the self-exemption this rule
exists to prevent.

| Module | Version | Licence | Serves | Shipped / build-time-only |
|---|---|---|---|---|
| github.com/google/go-cmp | v0.6.0 | BSD-3-Clause | lint | build-time-only |
| github.com/yuin/goldmark | v1.4.13 | MIT | lint | build-time-only |
| golang.org/x/mod | v0.39.0 | BSD-3-Clause | lint | build-time-only |
| golang.org/x/net | v0.58.0 | BSD-3-Clause | lint | build-time-only |
| golang.org/x/sync | v0.22.0 | BSD-3-Clause | lint | build-time-only |
| golang.org/x/sys | v0.47.0 | BSD-3-Clause | lint | build-time-only |
| golang.org/x/telemetry | v0.0.0-20260811182544-a038080d80e5 | BSD-3-Clause | lint | build-time-only |
| golang.org/x/tools | v0.49.0 | BSD-3-Clause | lint | build-time-only |
