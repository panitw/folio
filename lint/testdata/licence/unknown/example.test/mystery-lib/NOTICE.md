This stub module contains NO third-party code. It exists only to be
classified by Story 1.3's licence checker fixture graph (D-1.3.8),
deliberately carrying NO LICENSE file at all, so the checker's
unresolvable-licence path is proven to fail rather than pass silently
(AC29, V11).
