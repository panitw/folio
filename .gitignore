# ── OS ────────────────────────────────────────────────────────────
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db

# ── Editors / IDEs ────────────────────────────────────────────────
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
*.swp
*.swo
*~

# ── Environment / secrets ─────────────────────────────────────────
.env
.env.*
!.env.example
*.pem
*.key

# ── Node / frontend (designer: Vite + React + TS) ─────────────────
node_modules/
dist/
build/
.vite/
*.tsbuildinfo
coverage/
.eslintcache
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.pnpm-store/

# ── Go ────────────────────────────────────────────────────────────
bin/
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
vendor/
go.work
go.work.sum

# ── Cross-target matrix harness build output (Story 1.2, AC12/F-7) ─
folio-go/.matrix-build/

# ── Story 1.3's licence-check fixture module paths deliberately live
# under `example.test/` (D-1.3.8 — RFC 2606's reserved TLD, used so a
# fixture's fake module path can never collide with a real one). `*.test`
# above, meant only for compiled Go test binaries, would otherwise
# silently swallow the whole directory (and everything AC29/AC30 rely on
# being committed) — found while staging this story's commit. Un-ignore
# it explicitly.
!lint/testdata/**/example.test/
!lint/testdata/**/example.test/**

# ── WASM build output ─────────────────────────────────────────────
*.wasm
wasm_exec.js
folio-designer/src/generated/runtime/
folio-designer/src/generated/offline-assets.ts
folio-designer/src/generated/runtime-fonts.css

# ── Rendering / test output ───────────────────────────────────────
tmp/
.tmp/
*.log

# ── Tooling ───────────────────────────────────────────────────────
.claude/settings.local.json

# ── BMAD (personal, installer-managed) ────────────────────────────
_bmad/config.user.toml

# Upstream VARIABLE font builds, fetched only to replay the shipped
# faces' derivation (`make fonts` / `make fonts-verify`, D-2.2.4). They
# are inputs, not artifacts: ~20 MB of sources for the ~11 MB of static
# faces that actually ship and ARE committed. Each face's NOTICE.md
# records the release URL and the sha256 to fetch them by.
/.font-sources/

# Local fontgen toolchain (pinned Python 3.12.13 + fontTools 4.63.0), never committed
/.fontgen-venv/
